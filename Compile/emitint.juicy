ascii(int n) {
  return ascii(byte(n));
}

emit(int n) {
  if (n<10) {
    emit(ascii(n+48));
    return 1;
  }
  shiftr = n/10;
  lowest = n - (10 * shiftr);
  emit(shiftr);
  emit(ascii(lowest+48));
  return 1;
}


byte main() {
  emit(1234);
  return byte(0);
}
